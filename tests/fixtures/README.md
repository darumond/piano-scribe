# Test fixtures

Audio fixtures are generated deterministically by `tests/conftest.py`; no binary audio is committed.
Place only tiny, redistributable regression fixtures here if a future test cannot generate its input.
