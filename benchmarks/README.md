# DSP benchmarks

After installing the project, run `python benchmarks/benchmark_dsp.py`. The script is deliberately
small and compares equivalent peak-normalization operations. It is a baseline for future profiling,
not a performance claim.
